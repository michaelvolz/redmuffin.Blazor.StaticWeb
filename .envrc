# Disable global AGENTS.md for this repo
# This skips ~/.config/opencode/AGENTS.md but keeps project-level rules
export OPENCODE_DISABLE_GLOBAL_CONFIG=true

# Set .NET environment for Blazor development
export DOTNET_ENV=Development