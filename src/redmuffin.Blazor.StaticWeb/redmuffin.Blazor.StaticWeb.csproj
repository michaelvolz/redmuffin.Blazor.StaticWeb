<Project Sdk="Microsoft.NET.Sdk.BlazorWebAssembly">
  <PropertyGroup>
    <DOTNET_WATCH_LOG_PREFIX>Blazor</DOTNET_WATCH_LOG_PREFIX>
  </PropertyGroup>

  <!--
    CORE PROJECT SETTINGS
    Basic framework and language settings that apply to all configurations
  -->
  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <!--
    DEBUG CONFIGURATIONS: Optimized for fast builds and AI agent workflows
    Applies to both Debug and Debug-Sass configurations
    These settings prioritize compilation speed over runtime performance and bundle size
  -->
  <PropertyGroup Condition="('$(Configuration)|$(Platform)'=='Debug|AnyCPU') OR ('$(Configuration)|$(Platform)'=='Debug-Sass|AnyCPU')">
    <!-- Disable WASM SIMD optimizations for faster debug builds -->
    <WasmEnableSIMD>false</WasmEnableSIMD>
    <!-- Disable trimming to speed up compilation (larger bundle, but faster builds) -->
    <PublishTrimmed>false</PublishTrimmed>
    <!-- Suppress trim analysis warnings since trimming is disabled in debug -->
    <SuppressTrimAnalysisWarnings>true</SuppressTrimAnalysisWarnings>
  </PropertyGroup>

  <!--
    RELEASE CONFIGURATION: Full production optimizations
    These settings prioritize runtime performance and minimal bundle size
  -->
  <PropertyGroup Condition="'$(Configuration)|$(Platform)'=='Release|AnyCPU'">
    <!-- Enable AOT compilation for better runtime performance and size optimization -->
    <RunAOTCompilation>true</RunAOTCompilation>
    <!-- Enable WASM SIMD for better runtime performance in production -->
    <WasmEnableSIMD>true</WasmEnableSIMD>
    <!-- Enable trimming to reduce bundle size for production deployment -->
    <PublishTrimmed>true</PublishTrimmed>
    <!-- Use partial trimming mode for balance between size and compatibility -->
    <TrimMode>partial</TrimMode>
  </PropertyGroup>

  <ItemGroup>
    <PackageReference Include="Blazored.LocalStorage" />
    <PackageReference Include="LZStringCSharp" />
    <PackageReference Include="Markdig" />
    <PackageReference Include="Microsoft.AspNetCore.Components.Authorization" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly" />
    <PackageReference Include="Microsoft.AspNetCore.Components.WebAssembly.DevServer" PrivateAssets="all" />
    <PackageReference Include="Microsoft.AspNetCore.WebUtilities" />
    <PackageReference Include="Microsoft.Extensions.Http" />
  </ItemGroup>

  <!--
    SCSS COMPILATION PACKAGE
    BuildWebCompiler2022 is only included in Debug-Sass configuration for SCSS compilation
    CSS files are normally pre-compiled and committed to source control
    Use Debug-Sass configuration when you need to compile SCSS: dotnet build with configuration Debug-Sass
  -->
  <ItemGroup Condition="'$(Configuration)'=='Debug-Sass' And '$(OS)'=='Windows_NT'">
    <PackageReference Include="BuildWebCompiler2022" />
  </ItemGroup>

  <!-- Display SASS compiler status in build output -->
  <Target Name="DisplaySassCompilerStatus" BeforeTargets="CoreCompile" Condition="'$(Configuration)'=='Debug-Sass'">
    <Message Importance="high" Text="🎨 SASS Compiler Active - BuildWebCompiler2022 is compiling SCSS files to CSS" />
  </Target>

  <ItemGroup>
    <AdditionalFiles Remove="C:\Users\flynn\.nuget\packages\microsoft.visualstudio.threading.analyzers\17.8.14\build\AdditionalFiles\vs-threading.LegacyThreadSwitchingMembers.txt" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Remove="C:\Users\flynn\.nuget\packages\microsoft.visualstudio.threading.analyzers\17.8.14\build\AdditionalFiles\vs-threading.MainThreadAssertingMethods.txt" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Remove="C:\Users\flynn\.nuget\packages\microsoft.visualstudio.threading.analyzers\17.8.14\build\AdditionalFiles\vs-threading.MainThreadSwitchingMethods.txt" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Remove="C:\Users\flynn\.nuget\packages\microsoft.visualstudio.threading.analyzers\17.8.14\build\AdditionalFiles\vs-threading.MembersRequiringMainThread.txt" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\redmuffin.Blazor.StaticWeb.Common\redmuffin.Blazor.StaticWeb.Common.csproj" />
  </ItemGroup>

</Project>