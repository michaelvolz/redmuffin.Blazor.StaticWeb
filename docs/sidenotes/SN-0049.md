---
id: SN-0049
date: 2026-06-14
title: Remediate inline C# control flow in 12 razor files — Conditional, ForEach, StateView patterns ready
status: pending
---

Inline C# control flow inventory from 2026-06-14 audit. Infrastructure components
created and ready. See `docs/markup-purity-inventory.md` for detailed tracking.

## Control flow remediation (11 page files + 5 card components)

- `Features/ApiHealth/ApiHealth.razor` — 2 `@if` → **DONE**. Canonical example using all 6 infrastructure components (Conditional, ForEach, Spinner, ErrorBanner, EmptyState) + ViewModel + PageState<T>. Use as reference for all future page refactors.
- `Features/ArticlesPage/Articles.razor` — 2 `@if` (badge state + error guard) → `<Conditional>` + computed bool
- `Features/VideosPage/Videos.razor` — 2 `@if` (same pattern as Articles) → `<Conditional>` + computed bool
- `Features/WeatherPage/Weather.razor` — 1 `@if`/`else` + 1 `@foreach` → `<StateView>` with `PageState<WeatherForecast[]>` + `<ForEach>`
- `Features/IconsPage/Icons.razor` — 1 `@foreach` (icon iteration) → `<ForEach>`
- `Features/AuthPage/Redirect.razor` — 1 `@if` (token null guard) → `<Conditional>` + computed bool
- `Features/DebugPage/CacheReset/CacheReset.razor` — 1 `@if` (processing state) → `<Conditional>` + computed bool
- `Features/DebugPage/LocalStorageDebug.razor` — 8 `@if` + 1 `@foreach` + inline ternaries → ViewModel + `<StateView>` / `<Conditional>` + computed props
- `Features/Common/Components/RefreshBadge.razor` — 1 `@if` (text null guard) → `<Conditional>` + computed bool
- `Features/Common/Components/RaindropItemList.razor` — 2 `@if` + 1 `@foreach` (list component — permitted internally, prefer `<Conditional>` wrappers)
- `Features/Common/PageLoadSpeed/Components/AppStartMetricsView.razor` — 5 `@if` (config check + collapse + metrics) → `<Conditional>` + computed props
- `Features/Common/PageLoadSpeed/Components/PageLoadMetricsView.razor` — 4 `@if` → `<Conditional>` + computed props

## Card components (low priority — value>0 display checks)

- `WasmBootstrapCard.razor` — 5 `@if`
- `TimingMetricsCard.razor` — 5 `@if`
- `ResourceSizeCard.razor` — 4 `@if`
- `PerformanceBreakdownCard.razor` — 3 `@if`
- `MetricProgressBar.razor` — 1 `@if` (condition in parent component)

## Infrastructure components (ready)

- `Features/Common/Components/Conditional.razor` — replaces `@if`
- `Features/Common/Components/ForEach.razor` — replaces `@foreach`
- `Features/Common/Components/StateView.razor` — replaces loading/error/data `@if`/`@else`
- `Features/Common/Components/Spinner.razor` — loading indicator
- `Features/Common/Components/ErrorBanner.razor` — error display
- `Features/Common/Components/EmptyState.razor` — empty state display
- `Features/Common/Components/PageState.cs` — discriminated union model

## Clean files (no remediation needed)

`_Imports.razor`, `App.razor`, `MainLayout.razor`, `CookieFooter.razor`, `NavMenu.razor`,
`Logo.razor`, `FoundationExamples.razor`, `Home.razor`, `Counter.razor`, `MarkdownExamples.razor`
