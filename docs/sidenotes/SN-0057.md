---
id: SN-0057
date: 2026-08-03
title: "Future: Api internal redesign is separate backend work — out of client RiverBooks program"
status: pending
---

# Api internal redesign (not for now)

**Disposition:** out of the client modularization program. Possible future **backend** task only.

**What it is:** restructuring the Azure Functions project itself (folder layout, handlers, shared code inside Api, SWA-specific cleanup) without moving it into Modules.

**Why parked:** RiverBooks work targets WASM host + Modules. Api may improve on its own timeline; it must not be mixed into “finish modularization” PRs.

**Production impact:** server-side only for pure Api refactors; do not drag Api source into the client publish graph.

**Revisit when:** there is a dedicated Api/Functions PRD. Never as a dependency of Raindrop or host triad work.
