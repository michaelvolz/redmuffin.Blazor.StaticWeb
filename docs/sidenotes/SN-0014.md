---
id: SN-0014
date: 2026-04-05
title: Make the NuGet updater apply version changes automatically, then keep the final clean build test gate
status: dismissed
dismissed-reason: NuGet updater auto-apply implemented
---

Make `scripts/Update-PackageVersions.ps1` actually update `Directory.Packages.props` instead of only reporting changes, so the package-update workflow becomes one command to discover and apply version bumps.

Keep the final verification step as `dotnet clean && dotnet build --verbosity quiet && dotnet test`.
