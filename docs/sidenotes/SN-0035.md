---
id: SN-0035
date: 2026-05-11
title: Quality gates need a default config file for per-solution defaults like project name/path
status: dismissed
dismissed-reason: Obsoleted — SlnxProjectDiscovery auto-discovers source and test projects from .slnx, eliminating the need for per-solution defaults config. The zero-flag happy path (dotnet run -- all) works for both solutions.
---

We should add some kind of default config for our quality gates. When running
in the main solution it should have different defaults than the tools solution —
at minimum the default project name and path should differ. We need to think
about what makes sense config-wise, keeping it simple (KISS).
