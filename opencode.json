{
  "$schema": "https://opencode.ai/config.json",
  "plugin": ["opencode-snippets"],
  "formatter": {
    "dotnet-format": {
      "command": ["dotnet", "format", "--include", "$FILE"],
      "extensions": [".cs"]
    }
  }
}