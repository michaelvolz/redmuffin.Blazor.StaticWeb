{
  "$schema": "https://opencode.ai/config.json",
  "plugins": ["opencode-snippets"],
  "formatter": {
    "dotnet-format": {
      "command": ["dotnet", "format", "--include", "$FILE"],
      "extensions": [".cs"]
    }
  }
}